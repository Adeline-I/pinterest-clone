{% extends 'layouts/base.html.twig' %}

{% block title %}Modifier l'épingle "{{ pin.title }}"{% endblock %}

{% block body %}

	<div class="row">
		<div class="col-md-6 mx-auto">
			<h1 class="my-5 text-center">
				Modifier l'épingle "{{ pin.title }}"
				<hr>
			</h1>
			{# Insertion du fichier de formulaire par la fonction include  #}
			{{ include('pins/_form.html.twig', {'submitButtonText': 'Modifier'}) }}
		</div>
	</div>

{% endblock %}
